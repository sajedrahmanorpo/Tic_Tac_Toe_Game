#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    long long n , m, a;
    cin >> n >> m >> a;
    
    // long long flagStone_n = (n + a - 1) / a;
    // long long flagStone_m = (m + a - 1) / a;

    // long long totalFlagStone = (flagStone_n * flagStone_m);

    // double num = 5.3;
    // cout << ceil(num) << endl;

    long long A = (n/a)
    long long B=( m/a)

    
    cout << totalFlagStone;
    return 0;
}