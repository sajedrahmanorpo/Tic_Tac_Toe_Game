#include <stdio.h>
struct myStructure
{
    int myRoll;
    char myLetter;
    char myString[30];
};

int main()
{
    // struct myStructure s1, s2;
    // s1.myRoll = 12;
    // s1.myLetter = 'A';
    // s2.myRoll = 22;
    // s2.myLetter = 'B';
    // strcpy(s1.myString, "Some text");
    // printf("%d %d\n", s1.myRoll, s2.myRoll);
    // printf("%c %c\n", s1.myLetter, s2.myLetter);
    // printf("My string: %s\n", s1.myString);
    
    
    // struct mySturcture s3 = {14, 'A', "Some Text"};
    // s3.myRoll = 50;
    // s1.myLetter = 'B';
    // strcpy(s1.myString, "Randi Rona");

    // printf("%d %c %s\n", s1.myRoll, s1.myLetter, s1.myString);


    
    return 0;
}